# ADR

Add an ADR when you make a significant decision (library, architecture, irreversible change).
Use a simple format: Context / Decision / Consequences.

Latest decision: ADR-0014 binds capabilities to the active workspace/policy and
requires complete owned-process-group revocation before stop/uninstall succeeds.
